%Load the results for the 10D optimizations
if ~exist('sav10d', 'var')       
    load('sav10d.mat');
end

%Load the results for the 30D optimizations
if ~exist('sav30d', 'var')
    load('sav30d.mat');
end